{
  "name": "web-pandemic-proto",
  "version": "0.0.1",
  "description": "prototype for pandemic inspired implementation using web technologies",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "mkdirp build && browserify index.js -o build/index.js",
    "watch": "watchify index.js -o build/index.js",
    "livereload": "livereload ."
  },
  "author": "",
  "dependencies": {
    "fabric": "^1.6.3"
  },
  "devDependencies": {
    "browserify": "^13.1.0",
    "livereload": "^0.5.0",
    "mkdirp": "^0.5.1",
    "watchify": "^3.7.0"
  }
}
